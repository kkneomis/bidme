<div class="col-md-6 col-md-offset-3">  
    <%= form_for([@event, @bid]) do |f| %>
      <% if @bid.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@bid.errors.count, "error") %> prohibited this bid from being saved:</h2>

          <ul>
          <% @bid.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.text_field :price, placeholder:"How much would you like to bid for this event?", class:"form-control"%>
      </div>
      <div class="actions">
        <%= f.submit "Bid it!", class:"btn btn-primary" %>
      </div>
    <% end %>
</div>